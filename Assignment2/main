#include <iostream>
#include "DataSet.hpp"
#include "FrequencyTable.hpp"

using namespace std;

int main() {
    DataSet sampleData;
    sampleData.printData();

    FrequencyTable table(sampleData);
    table.buildTable();

    table.printTable();

    return 0;
}
